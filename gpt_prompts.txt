MODULE CONTENT GENERATION:
Provide an explanation of the topic: Understanding Compilation and Execution, keep consise but understandable. The text should be in both SSML (for speech synthesis) and normal MARKDOWN format. Make use of the SSML format as perfectly as possible and also make use of MARKDOWN format as perfectly as possible. Start the SSML format with "SSML:" and MARKDOWN text format with "TEXT:". Include only one code example, and start code block with "CODE:". Please strictly use this format only. Do not use **SSML:** or **TEXT:** or **CODE:** to bold. Also DO NOT USE backquotes (`). Strictly use this format only.
